# M5Paper Core

ESP-IDF starter firmware for M5Paper with an OTA-ready partition table.

Initial firmware behavior: prints a heartbeat over serial once flashed.

## Current path

GitHub Actions is currently blocked by the GitHub account billing lock, so build and flash locally first:

```text
FLASH_LOCAL.md
```

After the first successful USB flash, OTA and release automation can be added back on top.
